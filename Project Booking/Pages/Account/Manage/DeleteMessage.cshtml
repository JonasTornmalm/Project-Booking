@page
@model Project_Booking.DeleteMessageModel
@{
    ViewData["Title"] = "DeleteMessage";
    ViewData["ActivePage"] = ManageNavPages.Inbox;
}

<h1>Delete Message</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Subject
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CurrentConversation.FirstOrDefault().Subject)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CurrentConversation.FirstOrDefault().Sender.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CurrentConversation.FirstOrDefault().Sender.Email)
        </dd>
    </dl>

    <form method="post">
        <input asp-route-id="@Model.CurrentConversation.FirstOrDefault().Conversation" type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="/Account/Manage/Inbox">Back to List</a>
    </form>
</div>